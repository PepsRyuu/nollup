let { nollup, fs } = require('../../nollup');

describe ('Options: output.format', () => {
    it ('should allow output.format to be defined')
    it ('should only allow esm and iife to be used');
    it ('should not allow dynamic imports for iife');
});