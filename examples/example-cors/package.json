{
    "scripts": {
        "clean": "rm -rf dist",
        "start": "node ../../lib/cli.js -c",
        "server": "http-server -c-1 -p 9000 public",
        "build": "npm run clean && rollup -c"
    },
    "devDependencies": {
        "http-server": "^0.12.3",
        "rollup": "^1.20.1"
    }
}
