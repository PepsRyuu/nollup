{
    "scripts": {
        "clean": "rm -rf dist",
        "start": "node ../../lib/cli.js -c --content-base public --port 9001",
        "build": "npm run clean && rollup -c"
    },
    "devDependencies": {
        "rollup": "^1.20.1"
    }
}
